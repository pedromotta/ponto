// const PunchIn = require('..')
const assert = require('chai').assert

describe('given init test', () => {
  it('should be true', () => {
    // const checkin = new PunchIn(1)
    // assert.equal(checkin.getHora(), 1)
    assert.equal(true, true)
  })
})
