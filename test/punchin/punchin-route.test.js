//const punchinRoute = require('../../src/app/punchin/punchin-route')
const assert = require('chai').assert

describe('given init punchin route  ', () => {
  it.skip('should be true', () => {
    console.log(punchinRoute.get())
    assert.equal(true, true)
  })
})
